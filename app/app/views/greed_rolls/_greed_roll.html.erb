<p><table width="30%" style="margin: 2px; border: 2px solid; border-collapse: collapse; text-align: center">
  <tr>
    <td rowspan="2" style="border: 1px solid">Roll <%= greed_roll.number %></td>
    <td rowspan="2" style="border: 1px solid">Score: <%= greed_roll.score %></td>
    <td style="border: 1px solid">Rolled:</td>
    <% greed_roll.dice.each do |rolled| %>
      <td width="20px"><%= rolled %></td>
    <% end %>
  </tr>
  <tr>
    <td style="border: 1px solid">Scoring?</td>
    <% greed_roll.scoring.each do |scoring| %>
      <td><%= if scoring then 'X' else '-' end %></td>
    <% end %>
  </tr>
</table></p>
