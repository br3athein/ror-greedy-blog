<h2>The Greed Session #<%= @greed_session.id %></h2>
<%= link_to 'Index', greed_sessions_path %>
<hr/>

<h3>(<%= @greed_session.players %> players)</h3>
<h4>Turn of player #<%= @greed_session.turn %></h4>

<% if @greed_session.legs.any? and @greed_session.legs.last.rolls.any? %>
Last roll:
<%= render @greed_session.legs.last.rolls.last %>
<% end %>
<%= render 'greed_rolls/controls' %>

<%= render @greed_session.legs %>
